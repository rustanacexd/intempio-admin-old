const event = {
  state: {
    currentEvent: ''
  },
  mutations: {
    SET_CURRENT_EVENT: (state, currentEvent) => {
      state.currentEvent = currentEvent
    }
  },
  actions: {}
}

export default event
